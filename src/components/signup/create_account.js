//use this in the backend for creating users
import './index.js'

const userName = document.getElementById("create-user-username");
const password = document.getElementById("create-user-password");


const createUser = () => {
    //check if it exists in the database
    //if not than create it
    //database stuff here
    //making it add to the user
    return userName;
}

export default createUser